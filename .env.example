# Single secrets file for dikw-data — used by BOTH data generation and `run_eval`.
# Copy to `.env` (gitignored) and fill in. codex uses OAuth, not a key here.
MINIMAX_API_KEY=
DEEPSEEK_API_KEY=
GITEE_API_KEY=
